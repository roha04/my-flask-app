# Placeholder until Railway CD is configured (plan phase 8).
# Push to main no longer triggers a deploy job.

name: Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: CD not configured yet
        run: echo "Railway CD workflow will replace this placeholder."
